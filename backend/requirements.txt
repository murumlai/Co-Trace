fastapi>=0.115.0
uvicorn[standard]>=0.30.6
python-multipart>=0.0.9
httpx>=0.27.2
pydantic>=2.11.0

# Optional: GitHub Copilot SDK provider (LLM_PROVIDER=copilot_sdk).
# Requires `copilot auth login` on the host. Left commented so the base
# install stays offline-friendly; uncomment to enable the Copilot provider.
# github-copilot-sdk==0.2.0
